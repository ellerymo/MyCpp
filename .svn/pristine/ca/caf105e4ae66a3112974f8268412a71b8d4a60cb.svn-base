#include<windows.h>
int __stdcall wWinMain(_In_ HINSTANCE hInstance, _In_opt_ HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow)
{
	//新手型
	while (true)
	{
		//找到qq窗口
		HWND hWnd=FindWindowW(L"TXGuiFoundation",L"QQ");//窗口句柄
		if (hWnd)
		{
			//移动窗口
			MoveWindow(hWnd, 800, 600, 100, 100, TRUE);
			//判断鼠标有没有移动到窗口上面
			//移动窗口嘿嘿嘿

		}

		

	}

	return 0;
}